# test file, contains comments which has to delete.
# to run 'python codeclf.py .' and check 'code_warning.json'

#sharps_after_charactermodel_inputs, sharps_after_charactermodel_inputs_index = fromTextToInputAndIndex(sharps_after_charactermodel)
#len(sharps_after_charactermodel_inputs), len(sharps_after_charactermodel_inputs_index), max(sharps_after_charactermodel_inputs_index)
#%%time
#sharps_after_character_predict_label = lstm_model.predict_classes(sharps_after_charactermodel_inputs)

#code_line_after_character = []
#mask = [np.squeeze(sharps_after_character_predict_label)==0] # code
#for lineno in np.asarray(sharps_after_charactermodel_inputs_index)[mask]:
#    code_line_after_character.append(sharps_after_charactermodel[lineno])
#len(code_line_after_character)

#code_line_after_character[:100]

def main():
    pass
    
if __name__ == '__main__':
    main()